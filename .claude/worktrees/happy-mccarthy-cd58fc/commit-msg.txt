feat: Listening Stats dashboard met Chart.js visualisaties

- Voeg routes/stats.js toe met 8 nieuwe aggregatie-endpoints:
  /api/stats/overview    — Last.fm + Plex samenvatting (plays, uren, artiesten, tracks)
  /api/stats/timeline    — Plays per dag/maand per periode
  /api/stats/genres      — Genre-verdeling gewogen op playcount (Last.fm tags)
  /api/stats/formats     — Audio-formaat breakdown uit Plex (FLAC/MP3/AAC/etc)
  /api/stats/top-artists — Top artiesten met thumbnails (Plex + Last.fm fallback)
  /api/stats/top-albums  — Top albums met covers via Last.fm
  /api/stats/top-tracks  — Top tracks (Plex primair, Last.fm fallback)
  /api/stats/library-health — Bibliotheek kwaliteitsindicatoren

- Voeg services/stats.js toe: genre-aggregatie, timeline builder, junk-filter

- Update db.js: stats_snapshots tabel voor dagelijkse trending-data

- Herbouw public/src/views/stats.js volledig:
  Rij 1: 4 overview cards (plays, uren, artiesten, tracks)
  Rij 2: Timeline bar chart + Genre donut chart met legenda
  Rij 3: Top artiesten horizontaal scrollbare bubbels (klikbaar naar detail)
  Rij 4: Top albums + top tracks ranked lijsten met cover art
  Rij 5: Bibliotheek gezondheid (format bar, enrichment ring, issue cards)
  Period selector: 7d / 30d / 3m / 12m / All Time
  Fallback naar /api/plex/stats als nieuwe endpoints niet beschikbaar zijn

services:
  lastfm-app:
    build: .
    container_name: lastfm-app
    restart: unless-stopped
    ports:
      - "9090:80"
    environment:
      - LASTFM_API_KEY=93d49bb56423f941a4b7a08778397670
      - LASTFM_USER=cjjansen13
      - PLEX_URL=http://host.docker.internal:32400
      - PLEX_TOKEN=CMsype6SErgKfVGGiiPg
    extra_hosts:
      - "host.docker.internal:host-gateway"
